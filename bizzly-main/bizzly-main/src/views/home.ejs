<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Bizzly</title>
    <link rel="stylesheet" href="/public/styles.css" />
  </head>
  <body>
    <header>
      <h1>Bizzly</h1>
      <p>Installed addons</p>
    </header>
    <main>
      <ul>
        <% (addons || []).forEach(addon => { %>
          <li>
            <a href="/addons/<%= addon.id %>"><%= addon.name %></a>
            <small>v<%= addon.version %></small>
          </li>
        <% }) %>
      </ul>
    </main>
  </body>
  </html>


